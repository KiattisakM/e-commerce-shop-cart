# ShopCart Implementation Plan (Enhanced v3)

**Created:** 2025-11-05 16:32
**Based on:** context_v2_20251105.md + ShopCart Pro research
**Session:** 20251105_163200
**Status:** Ready for execution
**Supersedes:** plan_20251105_162201.md

## 📊 Research-Driven Improvements

This plan incorporates findings from ShopCart Pro competitive analysis:
- ✅ Complete checkout flow (competitor has 404)
- ✅ Mini-cart drawer with feedback (competitor lacks this)
- ✅ Working filters + sort (competitor has sort only)
- ✅ Command palette for power users
- ✅ Optimized performance (fewer scripts than competitor)
- ✅ Better mobile UX (condensed layouts)
- ✅ 2 fonts max (competitor uses 3)

---

## 🎯 Project Overview

**Goal:** Build a modern, performant e-commerce shopping cart that surpasses ShopCart Pro's capabilities

**Tech Stack:**
- Next.js 16 + React 19
- Tailwind CSS 4 + shadcn/ui
- TypeScript (strict mode)
- Sanity CMS (headless)
- Framer Motion (animations)
- Zustand (state management)
- pnpm (package manager)

**Estimated Time:** 25-35 hours (enhanced scope)
**Phases:** 11 (added UX enhancements phase)

---

## 📋 Implementation Phases

### **Phase 0: Prerequisites** (30 min)

**Task 0.1: Verify pnpm Installation**
```bash
# Check if pnpm is installed
pnpm --version

# If not installed:
npm install -g pnpm

# Verify version (should be 8.x or 9.x)
pnpm --version
```

**Task 0.2: Verify Node.js Version**
```bash
# Next.js 16 requires Node.js 18.18.0 or higher
node --version

# Should output v18.18.0 or higher
```

**Success Criteria:**
- ✅ pnpm installed and accessible
- ✅ Node.js 18.18.0+ confirmed
- ✅ Working directory clean

---

### **Phase 1: Project Setup & Foundation** (2-3 hours)

**Task 1.1: Initialize Next.js 16 Project**
```bash
pnpm create next-app@latest . --typescript --tailwind --app --eslint --no-src-dir

# Answer prompts:
# - TypeScript: Yes
# - ESLint: Yes
# - Tailwind CSS: Yes
# - App Router: Yes
# - Customize default import alias: No (use @/*)
# - Turbopack: Yes
```

**Task 1.2: Install Core Dependencies**
```bash
pnpm add @sanity/client @sanity/image-url next-sanity
pnpm add zustand
pnpm add framer-motion
pnpm add clsx tailwind-merge class-variance-authority
pnpm add cmdk  # Command palette (⌘K)
pnpm add sonner  # Toast notifications
pnpm add lucide-react  # Icons
```

**Task 1.3: Install Dev Dependencies**
```bash
pnpm add -D @types/node @types/react @types/react-dom
pnpm add -D prettier prettier-plugin-tailwindcss
pnpm add -D @typescript-eslint/parser @typescript-eslint/eslint-plugin
```

**Task 1.4: Configure TypeScript (strict mode)**
```json
// tsconfig.json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true
  }
}
```

**Task 1.5: Setup Prettier**
```json
// .prettierrc
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

**Task 1.6: Configure Tailwind 4 with Custom Tokens**
```js
// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: ['class'],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#00bb7f',
          50: '#e6fff7',
          100: '#b3ffe6',
          500: '#00bb7f',
          600: '#009966',
          700: '#00774d',
        },
        cta: {
          DEFAULT: '#ff9800',
          light: '#ffb74d',
        },
      },
      fontFamily: {
        sans: ['var(--font-inter)', 'system-ui', 'sans-serif'],
        heading: ['var(--font-clash)', 'var(--font-inter)', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
export default config
```

**Success Criteria:**
- ✅ Next.js 16 project initialized with pnpm
- ✅ All dependencies installed
- ✅ TypeScript strict mode enabled
- ✅ Prettier + Tailwind plugin configured
- ✅ Custom emerald/green color tokens defined
- ✅ Dev server runs: `pnpm dev`

---

### **Phase 2: Sanity CMS Setup** (2-3 hours)

**Task 2.1: Create Sanity Project**
```bash
# Install Sanity CLI globally
pnpm add -g @sanity/cli

# Initialize Sanity in a 'studio' directory
cd ..
pnpm create sanity@latest

# Answer prompts:
# - Project name: shopcart-studio
# - Dataset: production
# - Project template: E-commerce (product catalog)

cd shopcart-by-claude
```

**Task 2.2: Configure Sanity Schemas**
Define schemas for:
- `product` (name, description, price, images, stock, category, badges)
- `category` (name, slug, description, image)
- `badge` (type: 'sale' | 'new' | 'hot', label, color)

**Task 2.3: Setup Sanity Client in Next.js**
```typescript
// lib/sanity/client.ts
import { createClient } from 'next-sanity'

export const client = createClient({
  projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
  dataset: process.env.NEXT_PUBLIC_SANITY_DATASET!,
  apiVersion: '2025-11-05',
  useCdn: false,
})
```

**Task 2.4: Create Sanity Environment Variables**
```bash
# .env.local
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your_token
```

**Task 2.5: Seed Sample Products**
Create 15-20 sample products in Sanity Studio with:
- Mix of categories
- Various price points
- Stock levels
- Sale/new badges
- High-quality images

**Success Criteria:**
- ✅ Sanity project created and configured
- ✅ Product, category, badge schemas defined
- ✅ Sanity client connected to Next.js
- ✅ 15-20 sample products seeded
- ✅ Images uploaded to Sanity CDN

---

### **Phase 3: Project Structure & Architecture** (1-2 hours)

**Task 3.1: Create Folder Structure**
```
app/
├── (shop)/                # Shop routes group
│   ├── layout.tsx         # Shop layout with header
│   ├── page.tsx           # Home/featured products
│   ├── shop/              # All products
│   ├── product/[slug]/    # Product detail
│   ├── cart/              # Cart page
│   └── checkout/          # Checkout flow
├── api/                   # API routes
├── globals.css
└── layout.tsx             # Root layout

components/
├── ui/                    # shadcn/ui components
├── layout/                # Header, Footer, Navigation
├── products/              # Product cards, grids, filters
├── cart/                  # Cart drawer, items, summary
├── checkout/              # Checkout steps, forms
└── shared/                # Shared components

lib/
├── sanity/                # Sanity client, queries
├── store/                 # Zustand stores
├── utils/                 # Utility functions
└── types/                 # TypeScript types

public/
└── fonts/                 # Custom fonts (if needed)
```

**Task 3.2: Define TypeScript Types**
```typescript
// lib/types/product.ts
export interface Product {
  _id: string
  name: string
  slug: string
  description: string
  price: number
  compareAtPrice?: number
  images: SanityImageSource[]
  stock: number
  category: Category
  badges?: Badge[]
  createdAt: string
}

export interface Category {
  _id: string
  name: string
  slug: string
  description?: string
  image?: SanityImageSource
}

export interface Badge {
  type: 'sale' | 'new' | 'hot' | 'lowstock'
  label: string
  color: string
}

// lib/types/cart.ts
export interface CartItem {
  product: Product
  quantity: number
}

export interface Cart {
  items: CartItem[]
  total: number
  subtotal: number
  itemCount: number
}
```

**Success Criteria:**
- ✅ Clean folder structure established
- ✅ All core type definitions created
- ✅ Routes planned and directories created

---

### **Phase 4: Design System & UI Foundation** (2-3 hours)

**Task 4.1: Install shadcn/ui CLI**
```bash
pnpm dlx shadcn@latest init

# Answer prompts:
# - Style: Default
# - Base color: Slate
# - CSS variables: Yes
```

**Task 4.2: Add shadcn/ui Components**
```bash
pnpm dlx shadcn@latest add button
pnpm dlx shadcn@latest add card
pnpm dlx shadcn@latest add badge
pnpm dlx shadcn@latest add input
pnpm dlx shadcn@latest add select
pnpm dlx shadcn@latest add sheet  # For drawers
pnpm dlx shadcn@latest add dialog
pnpm dlx shadcn@latest add toast
pnpm dlx shadcn@latest add checkbox
pnpm dlx shadcn@latest add separator
pnpm dlx shadcn@latest add command  # For command palette
```

**Task 4.3: Setup Fonts (Max 2 Families)**
```typescript
// app/layout.tsx
import { Inter } from 'next/font/google'
import localFont from 'next/font/local'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

const clash = localFont({
  src: '../public/fonts/ClashDisplay-Variable.woff2',
  variable: '--font-clash',
  display: 'swap',
})

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${inter.variable} ${clash.variable}`}>
      <body className="font-sans">{children}</body>
    </html>
  )
}
```

**Task 4.4: Configure Design Tokens**
Update `globals.css` with:
- Color variables for light/dark mode
- Spacing scale
- Typography scale
- Animation tokens

**Success Criteria:**
- ✅ shadcn/ui installed and configured
- ✅ All needed components added
- ✅ 2 font families loaded (Inter + Clash Display)
- ✅ Design tokens in CSS variables
- ✅ Light/dark mode support ready

---

### **Phase 5: Core Layout Components** (3-4 hours)

**Task 5.1: Build Header Component**
```typescript
// components/layout/header.tsx
Features:
- Sticky positioning
- Logo
- Navigation links (Home, Shop, Hot Deals, Contact)
- Search bar with ⌘K hint
- Cart icon with item count badge
- Wishlist icon with count badge
- Mobile hamburger menu
- Responsive design
```

**Task 5.2: Build Command Palette**
```typescript
// components/layout/command-palette.tsx
Features:
- Opens with ⌘K (Cmd+K / Ctrl+K)
- Quick search products
- Navigation shortcuts
- Keyboard navigation
- Uses cmdk library
```

**Task 5.3: Build Mini-Cart Drawer**
```typescript
// components/cart/mini-cart-drawer.tsx
Features:
- Slides from right side
- Shows cart items with thumbnails
- Quantity controls
- Remove button
- Subtotal display
- "View Cart" and "Checkout" buttons
- Empty state
- Uses Sheet component from shadcn/ui
```

**Task 5.4: Build Toast System**
```typescript
// components/shared/toast-provider.tsx
Features:
- Success, error, info variants
- Product added to cart notifications
- Remove from cart confirmations
- Uses sonner library
```

**Task 5.5: Build Footer Component**
```typescript
// components/layout/footer.tsx
Features:
- Links (About, Contact, Terms, Privacy)
- Social media icons
- Newsletter signup (optional)
- Payment badges
- Service guarantees display
```

**Success Criteria:**
- ✅ Header with all features working
- ✅ Command palette opens with ⌘K
- ✅ Mini-cart drawer functional
- ✅ Toast notifications working
- ✅ Footer with service guarantees
- ✅ Mobile responsive

---

### **Phase 6: Product Display Components** (4-5 hours)

**Task 6.1: Build Product Card**
```typescript
// components/products/product-card.tsx
Features:
- Product image with hover zoom
- Badge overlays (sale, new, hot, low stock)
- Category tag
- Product name
- Price with strike-through if on sale
- Percentage savings display
- Stock count indicator
- "Add to Cart" button
- Quick view icon (optional)
- Wishlist heart icon
- Hover state animations
```

**Task 6.2: Build Product Grid**
```typescript
// components/products/product-grid.tsx
Features:
- Responsive grid (1 col mobile, 2-3 tablet, 4 desktop)
- Loading skeleton states
- Empty state
- Pagination or infinite scroll
```

**Task 6.3: Build Filter Sidebar**
```typescript
// components/products/filter-sidebar.tsx
Features:
- Category multi-select checkboxes
- Price range slider
- Stock status filter (in stock, low stock)
- Badge filter (sale, new, hot)
- Applied filters summary
- Clear all button
- Mobile: drawer version
- Desktop: fixed sidebar
```

**Task 6.4: Build Sort & Filter Controls**
```typescript
// components/products/sort-controls.tsx
Features:
- Sort dropdown (Name A-Z, Price low-high, Newest, Popular)
- View toggle (grid/list)
- Results count display
- Filter drawer toggle (mobile)
```

**Task 6.5: Build Product Detail Page**
```typescript
// app/(shop)/product/[slug]/page.tsx
Features:
- Image gallery with zoom
- Product info (name, price, description)
- Stock status
- Quantity selector
- Add to cart button
- Add to wishlist button
- Breadcrumb navigation
- Related products section
```

**Success Criteria:**
- ✅ Product cards with all badges and info
- ✅ Working filter system (multi-select)
- ✅ Applied filters display
- ✅ Sort functionality working
- ✅ Product detail page complete
- ✅ Responsive across devices

---

### **Phase 7: State Management & Cart Logic** (3-4 hours)

**Task 7.1: Create Cart Store**
```typescript
// lib/store/cart-store.ts
Features:
- Add item to cart
- Remove item from cart
- Update quantity
- Clear cart
- Calculate totals
- Persist to localStorage
- Item count badge updates
```

**Task 7.2: Create Wishlist Store**
```typescript
// lib/store/wishlist-store.ts
Features:
- Add/remove items
- Toggle wishlist
- Persist to localStorage
- Count badge updates
```

**Task 7.3: Create Filter Store**
```typescript
// lib/store/filter-store.ts
Features:
- Selected categories
- Price range
- Stock filters
- Badge filters
- Applied filters list
- Clear filters function
```

**Task 7.4: Implement Cart Actions**
```typescript
// components/cart/cart-item.tsx
Features:
- Quantity increment/decrement
- Remove item with confirmation
- Update cart totals
- Toast notifications on changes
```

**Task 7.5: Build Cart Page**
```typescript
// app/(shop)/cart/page.tsx
Features:
- Full cart items list
- Quantity controls
- Remove items
- Cart summary (subtotal, tax estimate, total)
- Continue shopping button
- Proceed to checkout button
- Empty cart state
- Coupon code input (optional)
```

**Success Criteria:**
- ✅ Zustand stores working
- ✅ Add to cart with toast feedback
- ✅ Cart updates immediately
- ✅ Mini-cart drawer syncs with cart page
- ✅ LocalStorage persistence
- ✅ Cart page fully functional

---

### **Phase 8: Checkout Flow** (4-5 hours)

**⚠️ PRIORITY: Complete checkout (competitor's is broken)**

**Task 8.1: Design Linear Checkout Flow**
```
Step 1: Contact Info (email, guest checkout option)
Step 2: Shipping Address (with auto-save)
Step 3: Shipping Method
Step 4: Payment Info
Step 5: Review & Place Order
```

**Task 8.2: Build Checkout Layout**
```typescript
// app/(shop)/checkout/layout.tsx
Features:
- Progress indicator (steps 1-5)
- Order summary sidebar (sticky on desktop)
- Back button to previous step
- Save progress to localStorage
```

**Task 8.3: Build Contact Info Step**
```typescript
// app/(shop)/checkout/page.tsx
Features:
- Email input with validation
- Guest checkout vs. account creation
- Auto-save to localStorage
- Continue button (validates before proceeding)
```

**Task 8.4: Build Shipping Address Step**
```typescript
// app/(shop)/checkout/shipping/page.tsx
Features:
- Address form fields
- Country/state select dropdowns
- Postal code validation
- Save address option
- Auto-save as user types
- Continue button
```

**Task 8.5: Build Payment Step (Mock for MVP)**
```typescript
// app/(shop)/checkout/payment/page.tsx
Features:
- Mock payment form (for MVP)
- Card number input (validation only)
- Expiry and CVV fields
- Billing address same as shipping checkbox
- Place order button
```

**Task 8.6: Build Order Confirmation**
```typescript
// app/(shop)/checkout/success/page.tsx
Features:
- Order number display
- Order summary
- Estimated delivery date
- Email confirmation message
- Continue shopping button
- Clear cart after order
```

**Success Criteria:**
- ✅ Linear checkout flow (no confusing steps)
- ✅ Auto-save at each step
- ✅ Guest checkout available
- ✅ Progress indicator working
- ✅ Validation at each step
- ✅ Order confirmation page
- ✅ NO 404 errors (unlike competitor!)

---

### **Phase 9: UX Enhancements** (2-3 hours)

**NEW PHASE: Features competitor lacks**

**Task 9.1: Implement Keyboard Shortcuts**
```typescript
// hooks/use-keyboard-shortcuts.ts
Features:
- ⌘K: Open command palette
- ⌘B: Open cart drawer
- ⌘W: Open wishlist
- ESC: Close drawers/modals
- /: Focus search
```

**Task 9.2: Add Loading States**
```typescript
Features:
- Product card skeletons
- Page transition indicators
- Button loading spinners
- Optimistic UI updates
```

**Task 9.3: Build Social Proof Components**
```typescript
// components/shared/social-proof.tsx
Features:
- Customer count badge
- Trust indicators (secure checkout, money-back)
- Service guarantees grid
- Payment method logos
```

**Task 9.4: Add Microinteractions**
```typescript
Features:
- Hover animations on cards
- Cart button success animation
- Wishlist heart fill animation
- Page transitions with Framer Motion
- Smooth scroll behavior
```

**Task 9.5: Optimize Mobile UX**
```typescript
Features:
- Bottom sheet for filters (mobile)
- Sticky "Add to Cart" bar (product page mobile)
- Swipe gestures for cart items
- Condensed product cards (mobile)
- Progressive disclosure (show more/less)
```

**Success Criteria:**
- ✅ Keyboard shortcuts working
- ✅ All loading states implemented
- ✅ Social proof visible
- ✅ Smooth animations throughout
- ✅ Mobile UX optimized (better than competitor)

---

### **Phase 10: Performance & SEO** (2-3 hours)

**Task 10.1: Optimize Images**
```typescript
Features:
- Use Next.js Image component everywhere
- Lazy load below-the-fold images
- WebP format with fallbacks
- Responsive image sizes
- Blur placeholder while loading
```

**Task 10.2: Implement Metadata & SEO**
```typescript
// app/(shop)/page.tsx
export const metadata = {
  title: 'ShopCart - Modern E-commerce',
  description: 'Shop the latest products...',
  openGraph: { ... },
}

Features:
- Dynamic metadata per page
- Structured data (Product schema)
- sitemap.xml
- robots.txt
```

**Task 10.3: Optimize Bundle**
```bash
# Analyze bundle size
pnpm add -D @next/bundle-analyzer

Features:
- Code splitting
- Dynamic imports for heavy components
- Remove unused dependencies
- Tree shake libraries
```

**Task 10.4: Add Analytics (Minimal)**
```typescript
// Only essential analytics, NOT like competitor's heavy scripts
Features:
- Vercel Analytics (built-in)
- Simple page view tracking
- NO Google Ads, Tag Manager, or heavy third-party scripts
```

**Task 10.5: Performance Testing**
```bash
# Lighthouse CI in dev
pnpm dlx lighthouse http://localhost:3000 --view

Targets:
- LCP < 2s
- FID < 100ms
- CLS < 0.1
- Mobile score > 90
```

**Success Criteria:**
- ✅ All images optimized with Next.js Image
- ✅ Metadata and SEO configured
- ✅ Bundle size < 200KB initial
- ✅ Lighthouse score > 90 (mobile & desktop)
- ✅ Faster than competitor

---

### **Phase 11: Testing & Deployment** (2-3 hours)

**Task 11.1: Manual Testing Checklist**
```
Desktop:
- [ ] Browse products
- [ ] Use filters and sort
- [ ] Add to cart with toast
- [ ] Open mini-cart drawer
- [ ] Update quantities
- [ ] Complete checkout flow
- [ ] Test command palette (⌘K)
- [ ] Test keyboard shortcuts
- [ ] Test light/dark mode

Mobile:
- [ ] Browse products (grid)
- [ ] Filter drawer
- [ ] Add to cart
- [ ] Cart page
- [ ] Checkout flow
- [ ] Header navigation
```

**Task 11.2: Browser Testing**
- Test in Chrome, Firefox, Safari
- Test on iOS and Android
- Verify touch interactions

**Task 11.3: Deploy to Vercel**
```bash
# Install Vercel CLI
pnpm add -g vercel

# Link project
vercel link

# Set environment variables in Vercel dashboard
# - NEXT_PUBLIC_SANITY_PROJECT_ID
# - NEXT_PUBLIC_SANITY_DATASET
# - SANITY_API_TOKEN

# Deploy
vercel --prod
```

**Task 11.4: Post-Deployment Verification**
- Test production URL
- Verify Sanity CMS connection
- Check all pages load
- Test checkout flow end-to-end
- Verify analytics working
- Run Lighthouse on production

**Success Criteria:**
- ✅ All manual tests passed
- ✅ Cross-browser compatibility verified
- ✅ Deployed to Vercel successfully
- ✅ Production site working correctly
- ✅ NO 404 errors (unlike competitor!)

---

## 📊 Comparison: Our MVP vs. ShopCart Pro

| Feature | ShopCart Pro | Our MVP |
|---------|--------------|---------|
| **Checkout Flow** | ❌ 404 Error | ✅ Complete & functional |
| **Cart Feedback** | ❌ Silent adds | ✅ Toast + mini-drawer |
| **Filters** | ❌ Sort only | ✅ Multi-select filters |
| **Mobile UX** | ⚠️ Long scroll | ✅ Condensed + progressive |
| **Performance** | ⚠️ Heavy scripts | ✅ Minimal external deps |
| **Command Palette** | ✅ Present | ✅ Enhanced with shortcuts |
| **Fonts** | ⚠️ 3 families | ✅ 2 families |
| **Dark Mode** | ❌ Not visible | ✅ Built-in |

---

## 🔧 pnpm Commands Reference

```bash
# Development
pnpm dev          # Start dev server (Turbopack)
pnpm build        # Build for production
pnpm start        # Start production server
pnpm lint         # Run ESLint
pnpm format       # Run Prettier

# Dependencies
pnpm add <pkg>            # Install dependency
pnpm add -D <pkg>         # Install dev dependency
pnpm remove <pkg>         # Remove dependency
pnpm install              # Install all dependencies
pnpm dlx <cmd>            # Execute package (like npx)

# Sanity
cd ../shopcart-studio
pnpm dev                  # Start Sanity Studio
pnpm build                # Build studio
pnpm deploy               # Deploy studio
```

---

## 📝 Execution Strategy

**Use `gogogo` shortcode to execute this plan in phases:**

1. Start with Phase 0-1 to get the project running
2. Complete Phase 2 to connect Sanity
3. Build UI foundation (Phases 3-5)
4. Add product features (Phase 6)
5. Implement cart & state (Phase 7)
6. **Priority:** Complete checkout (Phase 8) before other enhancements
7. Add UX enhancements (Phase 9)
8. Optimize & deploy (Phases 10-11)

**Or execute phases individually:**
- Read this plan
- Pick a phase
- Execute tasks sequentially
- Test after each phase
- Use git commits after major milestones

---

## ✅ Success Metrics

**Must Have (MVP):**
- ✅ Product browsing with filters
- ✅ Working cart with feedback
- ✅ Complete checkout flow (no 404s!)
- ✅ Mobile responsive
- ✅ Lighthouse score > 85

**Nice to Have (v1.1):**
- User accounts & order history
- Product reviews
- Related products AI
- Advanced search with AI
- Email notifications

**Competitive Advantages:**
- Complete checkout (competitor: broken)
- Mini-cart drawer (competitor: none)
- Working filters (competitor: sort only)
- Better performance (competitor: too many scripts)
- Better mobile UX (competitor: long scroll)

---

**Ready to execute with `gogogo`**
